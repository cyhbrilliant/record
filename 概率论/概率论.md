# 全概率公式
P(A) = P(A|B1) + P(A|B2) + P(A|B3)...
# 贝叶斯
P(Bi|A) = P(BiA)/P(A) = P(A|Bi)P(Bi)/P(A) = P(A|Bi)P(Bi)/(P(A|B1)P(B1) + P(A|B2)P(B2) + P(A|B3)P(B3)...)
# 离散型随机变量
随机变量X：X为不同的值即发生不同事件时，每个值都代表一个事件
分布律：随机变量X取不同值时的概率
# n重伯努力实验
p^k * (1-p)^(n-k)
当n取1时退化为0-1分布，即一次伯努力实验
P(X=k) = p^k * (1-p)^(1-k)
# 泊松分布
P(X=k) = lamda^k × e^(-lamda) / k!
X服从参数为lamda的泊松分布
当lamda=np（np为二项分布时的参数）近似为二项分布
泊松分布就是描述某段时间内，事件具体的发生的次数的概率。在频率附近概率最大，两边减小
# 随机变量分布函数
P(x1<X<=x2) = P(X<=x2) - P(X<=x1) = F(x2) - F(x1)
F(x) = P(X<=x)
# 连续型随机变量
概率密度函数f(x)每个点对应每个事件的概率相当于分布律
f(x)在负无穷到正无穷上的积分为1，即所有事件概论和为1
f(x)在负无穷到x上的积分为F(x)分布律
# 均匀分布
概率密度不变
# 指数分布
概率密度呈指数
描述事件发生的时间的概率，可从泊松分布中推导，即1-某段时间内发生次数为0的概率，则为事件在某段时间内发生的概率，随着间隔时间变长，事件的发生概率急剧下降，呈指数式衰减。
# 正态分布
当u=0,theta=1时为标准正态分布
非标准正态分布计算可用公式将X转为标准正态分布再计算
# 随机变量函数
离散型:求得Y所有可能的值可写出Y的分布律
连续型:用概率密度函数求得分布函数，将随机变量函数的y=写成x=后带入分布函数再对y求导得到新的概率密度函数。变量范围也要改变。
# 二维随机变量
P(x1 < X <= x2, y1 < y <= y2) = F(x2, y2) - F(X2, y1) + F(x1, y1) - F(x1, y2)
联合分布律
联合概率密度f(x,y) 事件X=x,Y=y发生概率为
边缘概率密度函数
# 两个随机变量函数的分布
z=x+y 则f(X)f(Y)带入X=x，Y=z-x后相乘对X积分即可
# 期望
即均值
连续型随机变量期望： 值×概率密度函数的积分
# 方差
方差定义：随机变量为(x-E(x))^2的均值
D(x) = E(x^2) - (E(x))^2
# 协方差，相关系数
相关只是表征线性相关，当两变量不相关不代表相互独立 y=x^2可以不相关但不代表独立
# 原点矩，中心距，混合距，混合中心距
期望：一阶原点矩
方差：二阶中心距
协方差：二阶混合中心距
# 协方差矩阵
n维正态随机变量
# 弱大数定律
独立同分布的随机变量当n很大时其算术平均接近于期望u
# 伯努利大数定律
n次独立实验中A发生的次数的平均值接近于A发生的的概率p
# 大数定律
都是用频率代替概率
# 中心极限定理
独立同分布的足够多的无论是什么分布的随机变量之和符合正态分布
利用中心极限定理计算二项分布的极限分布是正态分布
当试验次数多时可以化为标准正态分布估计概率
# GAMMA分布
即从头开始到第n次事件的发生的时间，该随机变量分布即为Gamma分布。
Gamma分布即为多个独立且相同分布（iid）的指数分布变量的和的分布。
单个指数分布描述间隔，相加n个则为n次事件总时间。
# BETA分布
分布未知而猜测分布
有alpha和beta两个参数，首先需要确定这两个参数以符合先验知识，每次实验正确增加alpha不正确增加beta，则分布会随着试验次数增加而趋于稳定。
与二项分布不同的是二项分布每一次实验中事件发生的概率都不变，两两事件相互独立
# Chi-Square分布
一种经验分布，即不知道样本的分布假设服从正态总体,通常用于假设检验，即检验分布是否符合数据或两数据分布是否相同。
假设x1,x2,x3..xn服从N(0,1)则x1^2+x^2+x3^2+..xn^2服从自由度为n的Chi-Square分布
# t分布
小样本较正态分布更好，可代替正态分布，区别于正态分布，t分布的参数多了自由度n而非u与theta
一个标准正态分布除以（一个Chi-Square分布除以它的自由度然后开根号）
# F分布
F分布是两个Chi-Square分布分布除以他们各自的自由度再相除
# 点估计
已知模型中有未知参数，利用点估计求参数
距估计：n个未知参数则求n个1-n阶距，然后将未知参数利用1-n阶距表示，最后求出样本的1-n阶距来代替总体的距
极大似然估计：求似然函数，求对数似然函数，对未知量求导=0时似然性最大
# 样本方差
/(n-1)而非/n
# 区间估计
1-a为置信区间表示theta在某个区间的概率大于1-a
# 单侧置信区间
u用t分布估计
方差用Chi-Square分布估计
# Bootstrap法
有放回的抽样统计，即每次并不能取到所有样本，多次重复（1000次）来估计总体的性质
# 随机过程
随机过程就是一条关于时间变化的曲线X(t)，X(t)表示在t时刻的随机实验值
一族无限多个随机变量称为随机过程
不同时间下的X(t)状态构成样本函数，所有不同实验结果构成一族
状态空间：一切X(t)可能取到的值
# 伯努利随机过程
掷筛子，1-n次每次得到不同的{1,2,3,4,5,6}点数即状态可构成X(t)曲线
# 连续型随机过程，离散型随机过程
根据随机变量的连续或离散，例如伯努利随机过程是离散型随机过程，因为状态有离散型取值不连续
# 分布函数族
X(t)称为分布函数，F(x,t)=P{X(t)<=x}称为分布函数族
不同时刻的n次实验的分布函数得到n维分布函数族，n越大统计信息越完善
均值u(x)指的是所有样本在t时刻时的均值
# 增量平稳性
当X(t+h)-X(s+h)与X(t)-X(s)具有相同分布则增量具有平稳性。这时X(t)-X(s)增量分布函数只依赖于t-s，和t，s本身无关，独立增量过程齐次
# 泊松过程
增量N(t)-N(to)记为N(t0,t)表示在时间间隔(t0,t]内发生的事件数。N(t)表示到此时已经发生的事件数。
Pk(t0,t)代表在t0到t时间内发生事件数为k的可能性
对于充分小的t-t0，发生一次事件的概率记为lamda * t，发生两次及以上事件的概率可看做高阶无穷小，忽略不计
N(0)=0
将N(t)记作强度为lamda的泊松过程，t1，t2...事件发生的时间点记作强度为lamda的泊松流
通过推导得出泊松随机过程的增量分布律Pk(t0,t) = (lamda×(t-t0))^k × e^(-lamda×(t-t0)) / k!即参数为lamda×(t-t0)的泊松分布
对于非均匀的泊松过程：即lamda强度随时间变化=>lamda(t)，可推导出即参数为lamda(t)dt在(t0,t)上对t积分的泊松分布
# Markov过程
时刻t0所处的状态可以决定t>t0时刻所处的状态
Markov性：将来的状态只依赖于现在的状态，与过去的状态无关
用条件分布描述：随机过程在 X(t1) - X(tn-1) 下 X(tn)的条件分布函数 等于 仅有条件X(tn-1)下X(tn)的条件分布函数，称此过程为Markov过程
泊松过程是时间连续状态离散的Markov过程
当时间与状态都是离散时称为Markov Chain
Pij(m,m+n) = P{Xm+n=aj | Xm=ai} 即时刻m有状态ai的条件下m+n时刻状态为aj的概率
从状态ai出发到时刻m+n的状态必然在a1，a2...中，故(j=1，j=+无穷)求和Pij(m,m+n) = 1，将ij写成二维矩阵形式记为Markov Chain转移概率矩阵，每一行之和为1
当转移概率Pij(m,m+n)与起始时间m无关只与状态i，j和步长n有关时记为Pij(n)，此时转移概率具有平稳性，并称此Chain是齐次的，当步长为1时就是一步转移概率：Pij=Pij(1)=P{Xm+1=aj | Xm=ai}
一步平稳转移概率是和当前的状态a有关而非与时刻有关，表格中每一行为不同的当前状态，如当前状态为a2则选择第二行，每一列代表了下一步转移到另一个状态的概率。
# Markov Chain多步转移概率
表示为Pij(n)
主要思想是利用Chapman-Kolmogorov方程即C-K方程将n步分解，Pij(n)分解为Pij(u+v)=(k=1，k=+无穷)求和Pik(u)×Pkj(v),也就是从S时刻的ai状态先经过u时间变为状态ak在经过v时间变成状态aj。也可以写成两矩阵相乘形式P(u+v)=P(u)行向量矩阵8 * P(v)列向量矩阵
从一步转移概率推导多步转移概率：P(n)=P(1)P(n-1)=PP(n-1)=P^n,即一步转移概率矩阵的n次方就是多步转移概率矩阵。得齐次的Markov Chain的有限维分布可有初始分布和一步转移概率完全确定
# Markov Chain利用初始分布求多步转移后的分布
已知初始分布求联合分布P{X0=0,X2=1}
分解为P{X0=0} * P{X2=1 | X0=0}
P{X2=1 | X0=0}为多步转移概率，利用一步转移概率求解即可
已知初始分布求转以后分布P{X2=1}时，写成P1(2)，利用公式Pk(n)= (i=1，i=n)求和Pi(0)Pik(n) ,相当于求出联合分布律后再求边缘概率，可以将P1(2)当做边缘概率即P{X0=0,X2=1}+P{X0=1,X2=1}+P{X0=2,X2=1}概率相加
# Markov Chain的遍历性
若有遍历性，则对于固定状态j，不管链从什么状态出发，通过长时间的转移到达j的概率总趋近于一个值。即当转移概率Pij(n)存在极限则此链具有遍历性，P^n具有极限，此时链的极限称为极限分布（平稳分布）。
当存在正整数m使得任意ij下Pij(m)>0成立则链具有遍历性
# 平稳随机过程
分布函数不随时间改变而改变，即过去的状态现在的状态都对未来的状态有很强的影响。
平稳随机过程的均值为常数，E[X(t)]=E[X(0)]








